# وب‌سایت خدمات سئو v2: پلتفرم چندزبانه و SEO بین‌المللی

این یک پروژه کامل و پیشرفته برای ساخت یک وب‌سایت خدمات سئو است که به طور کامل روی زیرساخت Edge شرکت Cloudflare (با استفاده از Workers و **D1 Database**) اجرا می‌شود. این پروژه با تمرکز بر **تجربه کاربری Mobile-First**، **عملکرد فوق‌العاده (Streaming HTML)**، و **SEO بین‌المللی (i18n)** ساخته شده و **کاملاً با پلن رایگان Cloudflare سازگار است.**

## ✨ ویژگی‌های کلیدی

-   **پلتفرم چندزبانه:** پشتیبانی کامل از زبان‌های فارسی، انگلیسی و عربی با مسیریابی `/:lang/`.
-   **SEO بین‌المللی:** تولید خودکار تگ‌های `hreflang` و نقشه سایت چندزبانه با `xhtml:link`.
-   **تجربه کاربری Mobile-First:** طراحی کاملاً واکنش‌گرا با هدر تطبیقی و منوی همبرگری.
-   **عملکرد فوق‌العاده:** استفاده از **Streaming HTML** برای کاهش چشمگیر TTFB و بهبود Core Web Vitals.
-   **پایگاه داده SQL:** استفاده از **Cloudflare D1** برای مدیریت محتوای چندزبانه.
-   **کشینگ هوشمند:** استفاده از KV برای کش کردن پاسخ‌های HTML در Edge.
-   **تست‌های خودکار جامع:** اسکریپت‌هایی برای تست مسیریابی، سئو، نقشه سایت و محتوای چندزبانه.

## 🚀 نصب و راه‌اندازی

### ۱. پیش‌نیازها

-   [Node.js](https://nodejs.org/) (نسخه 18.x یا بالاتر) و NPM.
-   حساب کاربری Cloudflare.

### ۲. نصب و پیکربندی اولیه

1.  **کلون کردن پروژه و نصب وابستگی‌ها:**
    ```bash
    git clone <repository_url>
    cd <repo_name>
    npm install
    ```

2.  **لاگین به Wrangler:**
    ```bash
    npx wrangler login
    ```

3.  **پیکربندی `wrangler.toml` (فقط برای اولین بار):**
    -   یک دیتابیس D1 ایجاد کنید: `npx wrangler d1 create seo_db`
    -   یک KV Namespace برای کش ایجاد کنید: `npx wrangler kv:namespace create CACHE_KV`
    -   فایل `wrangler.toml` را باز کرده و مقادیر `database_id` برای دیتابیس و `id` برای KV namespace را با مقادیر تولید شده در مراحل قبل جایگذاری کنید.

4.  **اعمال Schema به دیتابیس (فقط برای اولین بار):**
    ```bash
    npx wrangler d1 execute seo_db --file=./db/schema.sql
    ```

---

## 💻 توسعه محلی (Local Development)

برای اجرای سرور توسعه محلی، از دستور زیر استفاده کنید:

```bash
npm start
```

این دستور `wrangler dev` را اجرا کرده و سرور را روی `http://localhost:8787` راه‌اندازی می‌کند.

### آماده‌سازی دیتابیس محلی

برای اینکه محتوای نمونه در دیتابیس محلی شما بارگذاری شود، مراحل زیر را در یک ترمینال جداگانه اجرا کنید:

1.  **اجرای اسکریپت تولید محتوا:**
    این دستور فایل `db/seed-content.sql` را با محتوای چندزبانه تولید می‌کند.
    ```bash
    npm run build:content
    ```

2.  **Seed کردن دیتابیس محلی:**
    این دستور محتوای تولید شده را به دیتابیس D1 محلی شما وارد می‌کند.
    ```bash
    npm run db:seed
    ```

---

## ☁️ استقرار در Cloudflare

برای استقرار وب‌سایت در شبکه جهانی Cloudflare، از دستور زیر استفاده کنید:

```bash
npm run deploy
```

این دستور به طور خودکار:
1.  پروژه را بیلد می‌کند (دارایی‌ها و محتوای seed را تولید می‌کند).
2.  Worker را در حساب Cloudflare شما پابلیش می‌کند.
