# وب‌سایت خدمات سئو روی Cloudflare Workers

این یک پروژه کامل برای ساخت یک وب‌سایت خدمات سئو است که به طور کامل روی زیرساخت Edge شرکت Cloudflare (با استفاده از Workers و KV Storage) اجرا می‌شود. این پروژه با تمرکز بر سرعت، سئو و معماری مدرن جاوااسکریپت (ESM) ساخته شده است.

## ✨ ویژگی‌ها

- **اجرا روی Edge:** سرعت بسیار بالا در سراسر جهان با استفاده از Cloudflare Workers.
- **رندر سمت سرور (SSR):** تمام صفحات به طور کامل در سمت سرور رندر می‌شوند که برای سئو ایده‌آل است.
- **بهینه‌سازی سئو:** پشتیبانی کامل از متا تگ‌های داینامیک، JSON-LD (Structured Data)، Canonical URL، OpenGraph و Twitter Cards.
- **فایل‌های سئو خودکار:** تولید خودکار `sitemap.xml` و `robots.txt`.
- **پنل مدیریت:** یک داشبورد ساده و امن برای مدیریت پست‌های وبلاگ (CRUD).
- **API داخلی:** Endpoints برای مدیریت محتوا که توسط پنل ادمین استفاده می‌شود.
- **معماری ماژولار:** کدها به صورت ماژولار و تمیز در پوشه‌های جداگانه سازماندهی شده‌اند.
- **پایگاه داده KV:** استفاده از Cloudflare KV Storage برای ذخیره‌سازی داده‌ها.
- **آماده Deploy:** پروژه کاملاً پیکربندی شده و آماده `wrangler publish` است.

## 🛠️ پشته فناوری (Tech Stack)

- **محیط اجرا:** Cloudflare Workers
- **مسیریابی (Routing):** `itty-router`
- **موتور قالب (Template Engine):** `eta`
- **دیتابیس:** Cloudflare KV Storage
- **ابزار Deploy:** `wrangler` CLI
- **طراحی:** TailwindCSS (از طریق CDN)

## 🚀 نصب و راه‌اندازی

برای راه‌اندازی این پروژه، مراحل زیر را دنبال کنید.

### ۱. پیش‌نیازها

- [Node.js](https://nodejs.org/) (نسخه 16.x یا بالاتر)
- [NPM](https://www.npmjs.com/)
- نصب `wrangler` CLI به صورت گلوبال:
  ```bash
  npm install -g wrangler
  ```
- لاگین به حساب Cloudflare:
  ```bash
  wrangler login
  ```

### ۲. نصب پروژه

1.  پروژه را کلون کرده و وارد پوشه آن شوید:
    ```bash
    git clone <repository_url>
    cd cloudflare-seo-site
    ```
2.  وابستگی‌ها را نصب کنید:
    ```bash
    npm install
    ```

### ۳. پیکربندی

مهم‌ترین بخش راه‌اندازی، پیکربندی فایل `wrangler.toml` است.

#### ایجاد KV Namespace

این پروژه از KV Storage برای ذخیره پست‌های وبلاگ استفاده می‌کند. شما باید دو Namespace ایجاد کنید: یکی برای محیط پروداکشن و دیگری برای تست محلی (preview).

1.  **برای پروداکشن:**
    ```bash
    wrangler kv:namespace create POSTS
    ```
    `id` که در خروجی نمایش داده می‌شود را کپی کرده و در `wrangler.toml` در بخش `id` قرار دهید.

2.  **برای تست محلی:**
    ```bash
    wrangler kv:namespace create POSTS --preview
    ```
    `id` که در خروجی نمایش داده می‌شود را کپی کرده و در `wrangler.toml` در بخش `preview_id` قرار دهید.

#### تنظیم متغیرهای امنیتی (Secrets)

برای پنل مدیریت، باید نام کاربری و رمز عبور را به صورت امن تعریف کنید.

```bash
wrangler secret put ADMIN_USER
# سپس نام کاربری مورد نظر را وارد کنید

wrangler secret put ADMIN_PASS
# سپس رمز عبور مورد نظر را وارد کنید
```

همچنین می‌توانید یک توکن امن برای API تعریف کنید:
```bash
wrangler secret put API_TOKEN
```

### ۴. اجرای محلی (Local Development)

با دستور زیر می‌توانید پروژه را روی سیستم خود اجرا کنید:

```bash
npm start
```

یا معادل آن:

```bash
wrangler dev
```

سایت روی آدرس `http://localhost:8787` در دسترس خواهد بود.

- **ورود به داشبورد:** به آدرس `http://localhost:8787/dashboard` بروید.
- **نام کاربری پیش‌فرض:** `admin`
- **رمز عبور پیش‌فرض:** `password`
  (این مقادیر در بخش `[dev]` فایل `wrangler.toml` قابل تغییر هستند).

### ۵. Deploy نهایی

پس از اینکه تمام تنظیمات را در `wrangler.toml` انجام دادید و secrets را ست کردید، با دستور زیر پروژه را روی Cloudflare منتشر کنید:

```bash
npm run deploy
```

یا معادل آن:

```bash
wrangler publish
```

پس از چند ثانیه، وب‌سایت شما روی دامنه اختصاصی Worker شما فعال خواهد شد.
