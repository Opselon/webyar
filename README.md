در ادامه پرامتی آماده کرده‌ام که می‌تونی مستقیم در ChatGPT یا هر مدل تولید کد مشابه استفاده کنی تا **یک سایت کامل خدمات سئو** برای Cloudflare Workers (با جاوااسکریپت مدرن و معماری ماژولار MJS) بسازه. این پرامت شامل الزامات فنی، ساختار سئو، و ویژگی‌های مدیریتی است تا خروجی نهایی حرفه‌ای و قابل توسعه باشه.

---

### 🎯 پرامپت نهایی (برای تولید سایت کامل سئو روی Cloudflare Worker)

> **پرامپت:**
>
> ساخت یک وب‌سایت کامل خدمات سئو با جاوااسکریپت مدرن (MJS) برای اجرا روی **Cloudflare Workers**.
> سایت باید **ماژولار، دارای پنل مدیریت (Admin Panel)، کاملاً سئو-فرندلی، سریع، و قابل توسعه** باشد.
> هدف: یک وب‌سایت خدمات SEO که به‌سرعت در گوگل ایندکس و رتبه بگیرد.
>
> ---
>
> #### 🔧 الزامات فنی:
>
> * استفاده از **جاوااسکریپت مدرن با ساختار ماژول‌ها (ESM / .mjs)**
> * اجرا در **Cloudflare Workers** با معماری edge-first
> * رندر سمت سرور (SSR) برای سئو بهتر
> * پشتیبانی از **meta tags، structured data (JSON-LD)**، sitemap و robots.txt
> * طراحی بهینه برای **Core Web Vitals** (LCP، CLS، FID)
> * استفاده از Routing ماژولار برای صفحات:
>
>   * `/` — صفحه اصلی (معرف خدمات سئو)
>   * `/services` — معرفی سرویس‌ها با جزئیات
>   * `/pricing` — پلن‌های قیمتی
>   * `/blog` — مقالات سئو (با رندر سمت سرور و URL سئو)
>   * `/contact` — فرم تماس با ارسال به email API
>   * `/dashboard` — پنل مدیریت (ورود، مدیریت پست‌ها و متا‌دیتا)
>
> ---
>
> #### 💡 ویژگی‌های فنی:
>
> * سیستم routing داینامیک (مانند mini Express برای Workers)
> * قالب‌بندی صفحات با template literal یا HTML template engine (مثل Eta یا Nunjucks)
> * قابلیت cache هوشمند (Cloudflare KV / Durable Objects)
> * ذخیره تنظیمات سئو و پست‌ها در KV Storage
> * پنل ادمین با Auth (JWT یا Basic Auth)
> * سیستم وبلاگ با meta title، description، schema markup و slug URL
> * ساخت خودکار sitemap.xml و robots.txt
> * API endpoint برای CRUD پست‌ها (`/api/posts`, `/api/settings`)
>
> ---
>
> #### 🧩 معماری ماژولار:
>
> * `/src/routes/*.mjs` — ماژول‌های مربوط به صفحات
> * `/src/api/*.mjs` — endpointهای API
> * `/src/utils/*.mjs` — توابع کمکی (SEO utils، caching، response handler)
> * `/src/templates/*.html` — قالب‌های HTML با متادیتا
> * `/src/config.mjs` — تنظیمات پایه سایت
>
> ---
>
> #### 🎨 طراحی و UX:
>
> * طراحی مینیمال با TailwindCSS (درون فایل HTML inject شود)
> * سرعت بارگذاری بالا (load < 1s)
> * Mobile-first design
> * از فونت‌های سیستم و lightweight SVG icons استفاده شود
>
> ---
>
> #### 🚀 SEO Optimizations:
>
> * رندر کامل HTML در پاسخ سرور (بدون نیاز به JS برای محتوا)
> * meta tags داینامیک بر اساس صفحه و محتوا
> * structured data برای:
>
>   * LocalBusiness
>   * Article (برای وبلاگ)
>   * FAQPage (در صفحه خدمات)
> * تولید خودکار canonical URLs
> * تگ‌های OpenGraph و Twitter Card
>
> ---
>
> #### 🧱 موارد پیشرفته:
>
> * پشتیبانی از JSON API برای اتصال به ابزارهای آنالیز (Google Search Console / GA4)
> * امکان افزودن کدهای تحلیل مثل Google Tag Manager یا Matomo
> * تنظیم خودکار cache control headers برای سرعت
> * قابلیت deployment مستقیم با `wrangler publish`
>
> ---
>
> #### ✅ خروجی نهایی:
>
> * فول‌استک پروژه در ساختار پوشه‌بندی شده و آماده‌ی اجرا در Cloudflare Workers
> * حداقل یک صفحه کامل HTML برای هر route
> * فایل `wrangler.toml` برای deploy
> * مستندات مختصر نحوه نصب و اجرا
>
> ---
>
> **هدف نهایی:** ساخت وب‌سایتی مدرن، سریع و سئو محور برای ارائه خدمات SEO با بهترین ساختار فنی ممکن برای Cloudflare Workers.

---

می‌خوای من همین پرامپت رو تبدیل کنم به یک **فایل اجرایی واقعی (ساختار پوشه و کدهای پایه)** برایت بسازم تا فقط با `wrangler publish` بالا بیاد؟
(در اون حالت، من کل پروژه‌ی اولیه با SSR، routing، meta و panel رو برات تولید می‌کنم.)
